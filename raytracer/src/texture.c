#include "texture.h"
#include <stdlib.h>
int load_texture(const char* filename, texture_t* tex) {
    // Load PNG/JPEG file into texture struct
    return 0;
}
void sample_texture(const texture_t* tex, float u, float v, float* rgb) {
    // Bilinear sample
}